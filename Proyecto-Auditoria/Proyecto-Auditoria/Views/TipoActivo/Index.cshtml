@model IEnumerable<Proyecto_Auditoria.Models.TipoActivo>
@{
    ViewBag.Titulo = "Mantenimiento Tipos Activos";
}


<div class="row margin_bottom_15" > 
    <div class="col-md-12">
        <a class="btn btn-primary btn-small" asp-area="" asp-controller="TipoActivo" asp-action="Create">
            <i class="fa fa-plus"></i> <span>Crear nuevo tipo de activo</span>
        </a>
    </div>
           
</div>

<div class="row">
    <div class="col-md-12">
        <div class="white_shd full margin_bottom_30">
            <div class="full graph_head">
                <div class="heading1 margin_0">
                <h2>Listado de tipos de activos</h2>
                </div>
            </div>
            <div class="table_section padding_infor_info">
                <div class="table-responsive-sm">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Codigo</th>
                            <th>Descripcion</th>
                            <th>Acciones </th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.CodigoTipoActivo</td>
                                <td>@item.Descripcion</td>
                                <td>

                                    <a class="btn btn-success btn-xs" asp-action="Edit" asp-controller="TipoActivo" asp-route-id="@item.CodigoTipoActivo">
                                        <i class="fa fa-pencil white_color2"></i> 
                                    </a>

                                    @*<a class="btn btn-danger btn-xs" asp-action="Delete" asp-controller="Activo" asp-route-id="@item.CodigoActivo">
                                        <i class="fa fa-trash white_color2" ></i> 
                                    </a>*@                                    
                                 

                                </td>
                            </tr>   
                        }
                        
                    </tbody>
                </table>
                </div>
            </div>
        </div>
    </div>

</div>
